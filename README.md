# Admin Dashboard — Returns Management

Тестовое задание: Frontend Core / Architect

## 🚀 Как запустить проект

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере

4. Приложение автоматически перенаправит на `/return-orders` - страницу списка возвратов

## 📁 Архитектура проекта

Проект организован по принципу **Feature-Sliced Design (FSD)** с адаптацией под Next.js App Router:

```
├── app/                              # Next.js App Router (routing, layouts)
│   ├── favicon.ico
│   ├── globals.css                   # Глобальные стили
│   ├── layout.tsx                    # Root layout с провайдерами
│   ├── page.tsx                      # Главная страница (редирект)
│   └── return-orders/                # Роуты для модуля возвратов
│       ├── [id]/
│       │   └── page.tsx              # Страница деталей возврата
│       ├── page.tsx                  # Страница списка возвратов
│       └── ReturnsPageClient.tsx     # Client компонент страницы списка
│
├── entities/                         # Доменные сущности (бизнес-логика)
│   └── return-orders/                # Сущность "Возврат заказа"
│       ├── index.ts                  # Публичный API модуля
│       └── ui/
│           └── ReturnOrderStatusBadge/
│               ├── component.tsx     # Компонент бейджа статуса
│               ├── index.ts
│               ├── styles.ts
│               └── types.ts
│
├── features/                         # Фичи (пользовательские сценарии)
│   └── return-orders/                # Фича "Управление возвратами"
│       ├── index.ts                  # Публичный API фичи
│       └── ui/
│           └── ReturnsTable.tsx      # Таблица возвратов
│
└── shared/                           # Общий код (переиспользуемый)
    ├── api/                          # API клиенты и React Query хуки
    │   ├── index.ts                  # Публичный API
    │   ├── ReactQueryProvider.tsx    # React Query провайдер
    │   ├── product/                  # API для одного продукта
    │   │   ├── api.ts
    │   │   ├── hook.ts
    │   │   ├── index.ts
    │   │   └── types.ts
    │   ├── products/                 # API для списка продуктов
    │   │   ├── api.ts
    │   │   ├── hook.ts
    │   │   ├── index.ts
    │   │   └── types.ts
    │   ├── return-order/             # API для одного возврата
    │   │   ├── api.ts
    │   │   ├── hook.ts
    │   │   ├── index.ts
    │   │   └── types.ts
    │   ├── return-orders/            # API для списка возвратов
    │   │   ├── api.ts
    │   │   ├── hook.ts
    │   │   ├── index.ts
    │   │   └── types.ts
    │   └── status-history/           # API для истории статусов
    │       ├── api.ts
    │       ├── hook.ts
    │       ├── index.ts
    │       └── types.ts
    │
    │
    ├── lib/                          # Утилиты и хелперы
    │   ├── error-boundary.tsx        # Error Boundary компонент
    │   ├── error-handler.ts          # Обработка ошибок
    │   ├── formatters.ts             # Форматирование данных
    │   ├── index.ts                  # Публичный API
    │   ├── routes.ts                 # Централизованные маршруты
    │   └── utils.ts                  # Утилиты (cn, etc.)
    │
    ├── mock-server/                  # Mock сервер для разработки
    │   ├── data/
    │   │   ├── index.ts              # Публичный API данных
    │   │   ├── products.ts           # Mock данные продуктов
    │   │   ├── return-order-products.ts
    │   │   ├── return-order-status-history.ts
    │   │   └── return-orders.ts      # Mock данные возвратов
    │   └── libs/
    │       └── helpers.ts            # Вспомогательные функции
    │
    └── ui/                           # UI-кит (design system)
        └── components/               # Базовые компоненты
            ├── index.ts              # Публичный API компонентов
            ├── Badge/                # Компонент бейджа
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   └── types.ts
            ├── Button/               # Компонент кнопки
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   └── types.ts
            ├── Card/                 # Компонент карточки
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   ├── types.ts
            │   ├── CardHeader/       # Заголовок карточки
            │   │   ├── component.tsx
            │   │   ├── index.ts
            │   │   ├── styles.ts
            │   │   └── types.ts
            │   ├── CardContent/      # Контент карточки
            │   │   ├── component.tsx
            │   │   ├── index.ts
            │   │   ├── styles.ts
            │   │   └── types.ts
            │   └── CardFooter/      # Футер карточки
            │       ├── component.tsx
            │       ├── index.ts
            │       ├── styles.ts
            │       └── types.ts
            ├── Input/                # Компонент инпута
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   └── types.ts
            ├── LoadingSpinner/       # Компонент загрузки
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   └── types.ts
            ├── Tabs/                 # Компонент табов
            │   ├── component.tsx
            │   ├── index.ts
            │   ├── styles.ts
            │   └── types.ts
            └── Typography/           # Компонент типографики
                ├── component.tsx
                ├── index.ts
                ├── styles.ts
                └── types.ts
```

### Почему такая структура?

1. **Разделение ответственности**: Каждый слой решает свою задачу
   - `app/` - только роутинг и layout
   - `entities/` - чистая бизнес-логика без UI
   - `features/` - пользовательские сценарии с UI
   - `shared/` - переиспользуемый код

2. **Масштабируемость**: Легко добавлять новые модули
   ```
   entities/
     ├── return-orders/
     ├── inventory/      # Новый модуль
     └── incident/       # Новый модуль
   
   features/
     ├── return-orders/
     ├── inventory/      # Новый модуль
     └── incidents/     # Новый модуль
   ```

3. **Изоляция**: Каждый модуль самодостаточен и может развиваться независимо

4. **Переиспользование**: UI-кит в `shared/ui` доступен всем модулям

5. **Централизация**: Маршруты в `shared/lib/routes.ts`, API структурирован по доменам

### Как масштабировать на другие модули?

Для добавления нового модуля (например, "Инциденты"):

1. Создать сущность: `entities/incident/types.ts`
2. Создать фичу: `features/incidents/` с API и UI
3. Добавить роуты: `app/incidents/page.tsx`
4. Добавить API модуль: `shared/api/incidents/`
5. Добавить mock данные: `shared/mock-server/data/incidents.ts`
6. Использовать существующий UI-кит из `shared/ui`

Все модули будут использовать общий:
- UI-кит (Button, Card, Badge, Input, Typography, Tabs)
- Обработку ошибок (Error Boundary, logError)
- React Query провайдер
- Design tokens (в `tailwind.config.ts`)
- Централизованные маршруты

## 🎨 UI-кит (Design System v0.1)

### Foundations

Все design tokens определены в `tailwind.config.ts`:

- **Цвета**: Primary, Success, Warning, Danger, Neutral (50-900 шкала)
- **Типографика**: Размеры (xs-4xl), веса (normal-bold), высоты строк
- **Spacing**: 4px сетка (4, 8, 12, 16, 24, 32px)
- **Radius**: sm, base, md, lg, xl, 2xl, full
- **Shadows**: sm, base, md, lg

### Компоненты

1. **Button** - Primary и Secondary варианты
2. **Input** - С поддержкой label и error состояния
3. **Badge** - Статусные лейблы (default, success, warning, danger, info)
4. **Card** - Контейнер с вложенными компонентами:
   - `Card.Header` - Заголовок карточки
   - `Card.Content` - Контент карточки
   - `Card.Footer` - Футер карточки
5. **Typography** - Типографика (h1-h6, body, caption)
6. **Tabs** - Компонент табов для фильтрации
7. **LoadingSpinner** - Индикатор загрузки

Все компоненты:
- Полностью типизированы (TypeScript)
- Используют design tokens из Tailwind
- Имеют простой API без избыточных флагов
- Следуют единой структуре (component.tsx, types.ts, styles.ts, index.ts)

## ✅ Что реализовано

### Основные требования

- ✅ **Архитектура**: Feature-Sliced Design структура
- ✅ **UI-кит**: 7 компонентов + design tokens в Tailwind
- ✅ **Список возвратов**: Таблица с колонками, фильтрация по статусу, сортировка
- ✅ **Детали возврата**: Полная информация, список товаров, история статусов
- ✅ **Обработка ошибок**: Error Boundary + logError функция
- ✅ **Состояния**: Loading, Error, Empty states
- ✅ **Централизованные маршруты**: Объект `routes` в `shared/lib/routes.ts`
- ✅ **Mock API**: Структурированные mock данные с разделением по доменам

### Технический стек

- ✅ React 18
- ✅ TypeScript
- ✅ Next.js 16 (App Router)
- ✅ Tailwind CSS 4
- ✅ TanStack Query (React Query)
- ✅ ESLint + Prettier

### Функциональность

**Страница списка возвратов** (`/return-orders`):
- Таблица с колонками: ID заказа, клиент, статус, сумма, дата
- Фильтр по статусу через Tabs (Все, Ожидает, На проверке, Одобрено, Отклонено)
- Сортировка по дате создания
- Состояния загрузки и ошибок
- Клик по строке для перехода к деталям

**Страница деталей возврата** (`/return-orders/[id]`):
- Общая информация (ID, клиент, сумма, статус, дата)
- Таблица товаров (название, количество, цена, причина)
- История статусов с датами и комментариями
- Состояния загрузки и ошибок
- Кнопка "Назад к списку"

**Обработка ошибок**:
- Error Boundary на уровне приложения
- Понятные сообщения пользователю
- Кнопка "Повторить" для retry
- Функция `logError` для централизованного логирования

**API структура**:
- Разделение на модули: `return-orders`, `return-order`, `products`, `product`, `status-history`
- React Query хуки для каждого модуля
- Mock данные организованы по доменам в `shared/mock-server/data/`

## 🔮 Дальнейшее развитие проекта

### Ближайшие приоритеты

1. **Пагинация**
   - Классическая пагинация для списков >50 записей
   - Или виртуализация через react-window для больших объемов

2. **Поиск и фильтры**
   - Поиск по ID заказа, имени клиента
   - Фильтры по диапазону дат и сумм
   - Сохранение состояния фильтров в URL params

3. **Производительность**
   - React.memo для таблицы и тяжелых компонентов
   - Динамический импорт страниц
   - Оптимизация ре-рендеров

### UX улучшения

4. **Расширенная таблица**
   - Множественный выбор строк
   - Массовые операции (изменение статуса)
   - Экспорт данных
   - Настройка видимости колонок

5. **Бизнес-логика**
   - Изменение статусов возвратов
   - Добавление комментариев к статусам
   - История изменений

6. **Адаптивность**
   - Responsive дизайн
   - Мобильная версия таблицы (карточки)

### Developer Experience

7. **Тестирование**
   - Unit тесты для утилит и компонентов
   - Integration тесты для фич
   - E2E тесты критичных сценариев

8. **Документация**
   - Storybook для UI-компонентов
   - JSDoc для публичных API
   - Примеры использования

9. **Мониторинг**
   - Error tracking (Sentry)
   - Performance monitoring
   - User analytics

### Дополнительные модули

10. **Feature Flags**
    - Система для постепенного rollout фич
    - A/B тестирование

11. **Новые модули**
    - Инциденты
    - Инвентарь
    - Аналитика

12. **Интернационализация**
    - i18n поддержка
    - Мультиязычность

## 📝 Технические детали

### Mock API

Используется клиентская эмуляция API через `setTimeout`:
- Список возвратов: ~500ms задержка
- Детали возврата: ~300ms задержка
- Все запросы проходят через React Query с кешированием

Mock данные организованы в `shared/mock-server/data/`:
- `return-orders.ts` - данные возвратов
- `products.ts` - данные продуктов
- `return-order-products.ts` - связь возвратов и продуктов
- `return-order-status-history.ts` - история статусов

### Данные

20 тестовых возвратов с различными статусами:
- pending (5 записей)
- in_review (4 записи)
- approved (6 записей)
- rejected (5 записей)

### Стилизация

- Все компоненты используют Tailwind CSS
- Design tokens определены в `tailwind.config.ts`
- Цветовая палитра: Primary, Success, Warning, Danger, Neutral
- Spacing: 4px сетка

### Маршрутизация

Централизованное управление маршрутами через `shared/lib/routes.ts`:
```typescript
routes.HOME.getUrl()                    // '/'
routes.RETURN_ORDERS_LIST.getUrl()      // '/return-orders'
routes.RETURN_ORDERS.getUrl(id)         // '/return-orders/:id'
```

## 🛠️ Команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Запуск production
npm start

# Линтинг
npm run lint

# Автоисправление ошибок линтера
npm run lint:fix

# Форматирование кода
npm run prettier:fix
```

## 📄 Лицензия

Проект создан в рамках тестового задания.
